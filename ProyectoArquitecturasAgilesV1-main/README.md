Para ejecutar el proyecto:

Clonar el repositorio desde Github

Abrir una terminal y ejecutar:

node index.js (Para ejecutar el backend)


Abrir una nueva terminal (Sin cerrar la primera)

cd frontend
npm install (Para instalar las dependencias necesarias de node)
npm start (Para iniciar el frontend)